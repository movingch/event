# v73

- 관리자 로그인 방식을 문제가 없었던 해시 라우팅 방식으로 되돌렸습니다.
- ADMIN 버튼은 다시 `#/admin`으로 이동합니다.
- 로그인 성공 시 `sessionStorage` 인증 후 현재 관리자 화면을 즉시 다시 렌더링합니다.
- 메인 화면에는 백업 바로가기를 노출하지 않습니다.
- 구글시트 자동 동기화 및 별도 `backup.html`은 유지합니다.
- 캐시 문제 방지를 위해 `index.html`에서 `app.js?v=73`으로 불러옵니다.
